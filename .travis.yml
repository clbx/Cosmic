language: cpp

os:
  - osx
  - linux

compiler:
  - gcc
  - clang

sudo: required

addons:
  apt:
    packages:
      - libsdl2-dev
      update: true
  homebrew:
    packages:
      - sdl2
      update: true

addons:
  apt:
    packages: libsdl2-dev

script:
- cd src
- make
